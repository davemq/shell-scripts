#!/bin/ksh93

flags=""

# parse options
while getopts "m" opt
do
    case $opt in
        "m")
            if [[ -n "$flags" ]]
            then
                flags="$flags "
            fi
            flags="${flags}-m "
            shift
            ;;
    esac
done

fibx "${flags}! $*"
